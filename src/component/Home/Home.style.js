import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    h3: {
        marginBottom:"5%",
        color:"white"
    },
    h5: {
        color:"white",
        opacity:"0.7"
    },
})

export default useStyles;